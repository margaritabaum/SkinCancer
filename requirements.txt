# Colab already provides torch, torchvision, numpy, pandas, matplotlib, seaborn,
# scikit-learn and Pillow. These are the additions, plus pins for running locally.
torch>=2.0
torchvision>=0.15
timm==1.0.11
kaggle>=1.6
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
Pillow>=9.5
